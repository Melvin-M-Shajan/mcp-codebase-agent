# Core MCP + agent
mcp>=1.0
langgraph>=0.2
langchain-core>=0.2

# LLM + embeddings: Gemini, via langchain-google-genai (chosen instead of the PRD's
# OpenAI/Anthropic default -- see README "Deviations from PRD" for why).
langchain-google-genai>=2.0
google-genai>=1.0

# Vector store
pinecone>=5.0

# Eval + tracing
ragas>=0.2
langfuse>=2.36

# Chunking
tree-sitter>=0.22
tree-sitter-python>=0.22

# Repo introspection
gitpython>=3.1

# Utilities
python-dotenv>=1.0
pytest>=8.0
pytest-asyncio>=0.24
